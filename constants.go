package bamboo

const (
	apiUrl = "/rest/api"
	// API ENDPOINTS
	latestResults = "/latest/result"
	project       = "/projects"
	plan          = "/plan"
	buildSerivce  = "/result"
	chart         = "/chart"
	// QUERY KEYS
	maxResultsKey     = "max-result"
)
