package bamboo

const (
	ApiUrl = "/rest/api"
	// API ENDPOINTS
	LatestResults = "/latest/result"
	project       = "/latest/projects"
	plan          = "/latest/plan"
	buildSerivce  = "/result"
	chart         = "/chart"
	// QUERY KEYS
	maxResultsKey = "max-result"
)
